# Copy this file to .env.local and fill in real values.
# .env.local is gitignored — never commit real keys.
#
# Only the Supabase URL and the anon/publishable key belong here.
# NEVER put the service role key or database password in a client-side env
# var (see CLAUDE.md §9 and the `frontend-security` skill) — anything
# prefixed VITE_ is bundled into the browser build and is public.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# Optional — shown on the order PDF export header (Phase 6.6) when set.
# Plain public display text only (store name/address/phone/tax code), never
# a secret. Leave unset and the PDF simply omits the store-info block.
VITE_STORE_NAME=
VITE_STORE_ADDRESS=
VITE_STORE_PHONE=
VITE_STORE_TAX_CODE=
